export interface Trip {
  code: string;
  name: string;
  length: string;
  start: string;         //  change this from Date to string
  resort: string;
  perPerson: string;
  image: string;
  description: string;
}
